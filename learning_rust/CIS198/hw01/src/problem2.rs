// Problem 02: Matrix Multiplication

/// Represents a matrix in row-major order
pub type Matrix = Vec<Vec<f32>>;

/// computes the products of the inputs `mat1` and `mat2`.
pub fn  mat_mut(mat1: &Matrix, mat2: &Matrix) -> Matrix {
    // TODO
    unimplemented!();
}
